# Build output and local runtime state
dist/
.wrangler/
coverage/
node_modules/

# Generated by `wrangler types` (gitignored)
worker-configuration.d.ts

# Generated by the weekly brands job -- a machine-written index, reformatting it
# would produce a spurious diff on every run.
data/

# Vendored verbatim by scripts/build-jq-wasm.mjs; reformatting would break its
# --check (and the hash in manifest.json).
worker/mcp/jq/vendor/jq-glue.mjs
worker/mcp/jq/vendor/manifest.json

# npm owns the formatting of its own lockfile
package-lock.json

# Gitignored scratch space (see CLAUDE.md) -- never shipped, never reviewed
.local/
